[project]
name = "truncate"
version = "0.1.1"
description = "Truncate text at a boundary rather than mid-word."
requires-python = ">=3.10"
dependencies = []
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
